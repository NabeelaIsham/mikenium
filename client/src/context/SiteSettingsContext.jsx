import React,{cloneElement,useEffect,useState} from 'react';
import {getPublicSettings} from '../services/public-api';
import {applyPublicSiteSettings} from '../config/site-contact';
function setMeta(name,content){let element=document.head.querySelector(`meta[name="${name}"]`);if(!element){element=document.createElement('meta');element.name=name;document.head.appendChild(element)}element.content=content||''}
function applyDocumentSettings(settings){const identity=settings.identity||{},seo=settings.seo||{};if(seo.defaultTitle)document.title=seo.defaultTitle;setMeta('description',seo.description);setMeta('keywords',seo.keywords);setMeta('robots',seo.allowIndexing===false?'noindex, nofollow':'index, follow');if(identity.primaryColor)document.documentElement.style.setProperty('--site-primary',identity.primaryColor);if(identity.faviconUrl){let link=document.head.querySelector('link[rel="icon"]');if(!link){link=document.createElement('link');link.rel='icon';document.head.appendChild(link)}link.href=identity.faviconUrl}}
function applyPublicDom(settings){const others=settings.others||{},identity=settings.identity||{};document.querySelectorAll('footer .footer-about>img').forEach(image=>{if(identity.logoUrl&&image.getAttribute('src')!==identity.logoUrl)image.src=identity.logoUrl;if(identity.logoAlt&&image.alt!==identity.logoAlt)image.alt=identity.logoAlt});document.querySelectorAll('footer .footer-about>p').forEach(element=>{if(others.footerDescription&&element.textContent!==others.footerDescription)element.textContent=others.footerDescription});document.querySelectorAll('footer .footer-bottom>p').forEach(element=>{if(others.copyright&&element.textContent!==others.copyright)element.textContent=others.copyright});document.querySelectorAll('footer .footer-bottom>span').forEach(element=>{const hidden=others.showSystemStatus===false;if(element.hidden!==hidden)element.hidden=hidden})}
function Maintenance({settings}){const general=settings.general||{},maintenance=settings.maintenance||{};return <main className="site-maintenance"><img src={settings.identity?.logoUrl||'/assets/mikenium-logo-transparent.png'} alt={settings.identity?.logoAlt||general.siteName}/><span>PLANNED MAINTENANCE</span><h1>{maintenance.title}</h1><p>{maintenance.message}</p><a href={`mailto:${general.contactEmail}`}>{general.contactEmail}</a></main>}
export function SiteSettingsProvider({children}){const [state,setState]=useState({settings:null,version:0});useEffect(()=>{let mounted=true;getPublicSettings().then(settings=>{if(!mounted)return;applyPublicSiteSettings(settings);applyDocumentSettings(settings);setState({settings,version:1})}).catch(()=>{});return()=>{mounted=false}},[]);useEffect(()=>{if(!state.settings)return;const refresh=()=>applyPublicDom(state.settings);requestAnimationFrame(refresh);const observer=new MutationObserver(refresh);observer.observe(document.getElementById('root'),{childList:true,subtree:true});return()=>observer.disconnect()},[state.settings]);if(state.settings?.maintenance?.enabled)return <Maintenance settings={state.settings}/>;return cloneElement(children,{key:`site-${state.version}`})}
